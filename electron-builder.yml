appId: com.llm-controller-orlova.app
productName: LLM Controller Orlova
directories:
  output: release
  buildResources: build
files:
  - "dist/**/*"
  - "package.json"
extraResources:
  - from: "static"
    to: "static"
    filter:
      - "**/*"
linux:
  target:
    - AppImage
  category: Utility
  synopsis: "Manage local LLMs and diffusion models with Hugging Face integration"
asar: true
